# DevHub Pro - 快速开始

## 🚀 5分钟上手

### 前置要求
- Rust 1.70+ (`rustup`)
- Node.js 18+ (`node`, `npm`)

### 1. 克隆并进入项目
```bash
cd /Users/hutiefang/aiproject/env_mirror_tool
```

### 2. 安装依赖
```bash
# 安装前端依赖
cd frontend && npm install

# 返回根目录
cd ..
```

### 3. 开发模式运行
```bash
# 方式1: 直接运行 (会自动启动前端开发服务器)
cargo run

# 方式2: 手动分离运行
# 终端1:
cd frontend && npm run dev

# 终端2:
cargo run
```

### 4. 生产构建
```bash
cargo build --release

# 可执行文件:
# target/release/devhub (4.6MB)
```

---

## 🧪 测试

### 单元测试
```bash
cargo test
# 输出: 4 passed
```

### 功能测试
```bash
cargo run --example test_core_functions
```

---

## 📖 功能演示

### Python检测
应用启动后会自动检测Python环境:
- ✅ 安装状态
- ✅ 版本号
- ✅ 安装路径

### 镜像源配置
1. 点击"⚡ 测速"按钮测试5个中国镜像源
2. 最快的镜像会标记"🏆 最快"
3. 点击"应用"切换到选定的镜像源
4. 点击"🔄 恢复默认"还原系统配置

---

## 🎨 界面特性

- 🌓 自动适配浅色/深色主题
- 📱 响应式布局 (支持手机/平板/桌面)
- ⚡ 流畅动画和悬停效果
- 🎯 清晰的视觉反馈

---

## 🔧 常用命令

```bash
# 检查代码
cargo check

# 格式化代码
cargo fmt

# Linter检查
cargo clippy

# 清理构建产物
cargo clean

# 查看依赖树
cargo tree

# 前端开发服务器
cd frontend && npm run dev

# 前端生产构建
cd frontend && npm run build
```

---

## 📦 项目结构

```
.
├── src/                 # Rust后端代码
│   ├── core/            # 核心Trait定义
│   ├── services/        # 服务层实现
│   ├── tools/           # 工具实现(Python/Node/Java)
│   ├── commands/        # Tauri IPC接口
│   ├── main.rs          # 主入口
│   └── lib.rs           # 库导出
├── frontend/            # React前端代码
│   ├── src/
│   │   ├── App.tsx      # 主应用组件
│   │   ├── App.css      # 样式文件
│   │   └── main.tsx     # 前端入口
│   └── package.json     # 前端依赖
├── examples/            # 示例和测试
├── Cargo.toml           # Rust项目配置
├── tauri.conf.json      # Tauri配置
└── build.rs             # 构建脚本
```

---

## ❓ 常见问题

### Q: 编译时提示distDir不存在?
A: 先构建前端: `cd frontend && npm run build`

### Q: 如何调试前端?
A: 打开开发者工具 (Cmd+Option+I on macOS)

### Q: 如何添加新的工具支持?
A:
1. 在`src/tools/`下创建新文件(如`node.rs`)
2. 实现`ToolDetector`和`MirrorConfigurator` trait
3. 在`src/commands/mod.rs`添加对应的Tauri command
4. 在前端添加UI组件

### Q: 测速很慢?
A: 正常,每个镜像源超时时间是5秒,5个镜像最多25秒

---

## 🎯 下一步

1. ⭐ 运行 `cargo run` 看到界面
2. 🧪 运行 `cargo test` 确保测试通过
3. 📝 阅读 `PROJECT_COMPLETE.md` 了解架构
4. 🔨 根据需求添加新工具支持

---

**需要帮助?** 查看详细文档:
- `01_REQUIREMENTS.md` - 完整需求规格
- `02_ARCHITECTURE.md` - 架构设计
- `03_DETAILED_DESIGN.md` - 实施指南
- `PROJECT_COMPLETE.md` - 项目完成报告

---

## 👤 作者

**Frank Hu**
- 📧 邮箱: hutiefang@gmail.com
- 🐙 GitHub: https://github.com/hutiefang76
- 🦊 Gitee: https://gitee.com/hutiefang

## 📄 许可证

MIT License
